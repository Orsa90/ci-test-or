name: Simple CI Test - V2

on:
  push:
    branches:
      - main
      - feature/*

  pull_request:
    branches:
      -main
      - feature/*
      
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Simulate environment check
      run: |
        echo "Checking for required files..."
        if [ -f README.md ]; then
          echo "README exists. ✅"
        else
          echo "README missing! ❌"
          exit 1
        fi


    - name: Run test
      run: |
        echo "This is your first pipeline, Or!"
        echo "CI is working. Let's gooo 💥"
